{
  "name": "faderjs",
  "version": "1.0.0",
  "description": "Lightweight, no-dependency JavaScript slider. Makes use of CSS3 transitions.",
  "main": "fader.js",
  "keywords": [
    "lightweight",
    "slider",
    "carousel"
  ],
  "author": "Thomas Erbe <vizuaalog@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^0.41.4",
    "rollup-plugin-buble": "^0.15.0",
    "uglify-js": "^2.7.5",
    "webpack": "^2.2.0-rc.7"
  },
  "scripts": {
    "build": "rollup -c > dist/fader.js",
    "minify": "uglifyjs dist/fader.js --compress --mangle --screw-ie8 --output dist/fader.min.js",
    "build-prod": "npm run build && npm run minify"
  }
}
